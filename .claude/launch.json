{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "sandbox-web",
      "//": "Goes through scripts/dev.sh because tool runners do not inherit a login shell, and Node lives under fnm rather than in /usr/local/bin.",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["scripts/dev.sh"],
      "port": 3000
    }
  ]
}
