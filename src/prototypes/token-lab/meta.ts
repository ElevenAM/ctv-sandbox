import type { PrototypeMeta } from '@/prototypes/types'

export const meta: PrototypeMeta = {
  slug: 'token-lab',
  index: 'EX-02',
  title: 'Token Lab',
  tagline: 'Retune the design system live and watch every component follow',
  summary:
    'Proof that the tokens in globals.css are the single source of truth: drag a hue or a radius and every primitive in the library re-themes instantly, because none of them hard-code a colour. Doubles as the contrast checker — each pairing is scored against WCAG as you move it.',
  status: 'live',
  tone: 'jade',
  tags: ['design-system', 'tokens', 'accessibility', 'no-database'],
  updated: '2026-09-11',
  capabilities: [],
  brief: 'docs/prototypes/token-lab.md',
}
